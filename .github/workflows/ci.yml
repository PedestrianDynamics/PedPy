name: jpsreport-ci

on:
  schedule:
    # Runs at 2:00 am each day
    - cron: '00 2 * * *'
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        python-version: [ '3.8' ]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -VV
          python -m site
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -r requirements.txt

      - name: Check format
        if: matrix.os == 'ubuntu-latest'
        run: |
          ${{github.workspace}}/scripts/check-format.sh

      - name: Run pytests
        run: |
          python -m pytest -vv -s tests

      - name: Build wheel
        if: matrix.os == 'ubuntu-latest'
        run: |
          python -m build
          cd dist
          echo "wheel=$(ls *.whl)" >> $GITHUB_ENV          

      - name: Upload wheel as artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v3
        with:
          path: ${{github.workspace}}/dist/${{ env.wheel }}
          name: ${{ env.wheel }}

    outputs:
      wheel: ${{ env.wheel }}

  test:
    needs: build
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        python-version: [ '3.8' ]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Download wheel
        uses: actions/download-artifact@v3
        with:
          name: jpsreport-1.0.0-py3-none-any.whl

      - name: Install wheel
        run: |
          ls -la
          wheel=$(ls *.whl)
          python -m pip install --upgrade pip
          python -m pip install ${{github.workspace}}/$wheel
          
      - name: Test if install was successful
        run: |
          jpsreport --version
